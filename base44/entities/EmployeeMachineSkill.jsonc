{
  "name": "EmployeeMachineSkill",
  "type": "object",
  "properties": {
    "employee_id": {
      "type": "string",
      "description": "ID del empleado"
    },
    "machine_id": {
      "type": "string",
      "description": "ID de la m\u00e1quina"
    },
    "nivel_habilidad": {
      "type": "string",
      "enum": [
        "Principiante",
        "Intermedio",
        "Avanzado",
        "Experto"
      ],
      "default": "Intermedio",
      "description": "Nivel de habilidad con la m\u00e1quina"
    },
    "certificado": {
      "type": "boolean",
      "default": false,
      "description": "Si tiene certificaci\u00f3n"
    },
    "fecha_certificacion": {
      "type": "string",
      "format": "date",
      "description": "Fecha de certificaci\u00f3n"
    },
    "experiencia_anos": {
      "type": "number",
      "description": "A\u00f1os de experiencia"
    },
    "observaciones": {
      "type": "string",
      "description": "Observaciones adicionales"
    },
    "orden_preferencia": {
      "type": "number",
      "description": "Orden de preferencia (1-10, basado en maquina_X slot)"
    }
  },
  "required": [
    "employee_id",
    "machine_id"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "$or": [
        {
          "data.employee_id": "{{user.employee_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "manager"
          }
        },
        {
          "user_condition": {
            "role": "supervisor"
          }
        },
        {
          "user_condition": {
            "role": "T\u00e9cnico"
          }
        },
        {
          "user_condition": {
            "role": "rrhh"
          }
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}
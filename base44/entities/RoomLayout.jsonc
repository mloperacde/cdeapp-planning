{
  "name": "RoomLayout",
  "type": "object",
  "row_level_security": [
    {
      "operation": "read",
      "allow": "{{true}}"
    },
    {
      "operation": "create",
      "allow": "{{user.role == 'admin' or user.role == 'Admin'}}"
    },
    {
      "operation": "update",
      "allow": "{{user.role == 'admin' or user.role == 'Admin'}}"
    },
    {
      "operation": "delete",
      "allow": "{{user.role == 'admin' or user.role == 'Admin'}}"
    }
  ],
  "properties": {
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "room_name": {
      "type": "string"
    },
    "canvas_width": {
      "type": "number",
      "default": 1200
    },
    "canvas_height": {
      "type": "number",
      "default": 800
    },
    "layout_elements": {
      "type": "array",
      "items": {
        "type": "object"
      }
    },
    "room_polygon": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "description": "Puntos del polígono de la sala"
    },
    "floor_color": {
      "type": "string",
      "default": "#475569",
      "description": "Color del suelo de la sala"
    },
    "background_image_url": {
      "type": "string"
    },
    "scale_cm_to_px": {
      "type": "number",
      "default": 1.31,
      "description": "Multiplicador de escala para convertir medidas en cm a píxeles del layout"
    },
    "floor_locked": {
      "type": "boolean",
      "default": false,
      "description": "Si el suelo de la sala está anclado al fondo (no se puede mover accidentalmente)"
    },
    "status": {
      "type": "string",
      "enum": [
        "Borrador",
        "Aprobado",
        "Archivado"
      ],
      "default": "Borrador"
    },
    "linked_articles": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "article_id": {
            "type": "string",
            "description": "ID del artículo en la BD"
          },
          "article_code": {
            "type": "string",
            "description": "Código del artículo"
          },
          "article_description": {
            "type": "string",
            "description": "Descripción/nombre del artículo"
          },
          "linked_date": {
            "type": "string",
            "format": "date-time"
          }
        }
      },
      "description": "Artículos vinculados a este layout (varía según el artículo a producir)"
    },
    "element_inventory": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "element_type": {
            "type": "string",
            "description": "Tipo de elemento (machine, conveyor_belt, etc.)"
          },
          "label": {
            "type": "string",
            "description": "Etiqueta descriptiva del elemento"
          },
          "quantity": {
            "type": "number",
            "description": "Cantidad necesaria"
          },
          "auto_generated": {
            "type": "boolean",
            "default": true,
            "description": "Si fue auto-generado desde el layout"
          },
          "notes": {
            "type": "string",
            "description": "Notas adicionales sobre este elemento del inventario"
          },
          "instance_number": {
            "type": "number",
            "description": "Número de instancia dentro del tipo (1, 2, 3...)"
          },
          "marca": {
            "type": "string",
            "description": "Marca/número de identificación individual del elemento (ej: MESA-01)"
          },
          "source_element_id": {
            "type": "string",
            "description": "ID del elemento del layout que originó este item de inventario"
          }
        }
      },
      "description": "Inventario de elementos necesarios según la configuración del layout"
    }
  },
  "required": [
    "name",
    "room_name"
  ]
}

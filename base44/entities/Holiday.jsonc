{
  "name": "Holiday",
  "type": "object",
  "row_level_security": [
    {
      "operation": "read",
      "allow": "{{true}}"
    },
    {
      "operation": "create",
      "allow": "{{user.role == 'admin' or user.role == 'Admin'}}"
    },
    {
      "operation": "update",
      "allow": "{{user.role == 'admin' or user.role == 'Admin'}}"
    },
    {
      "operation": "delete",
      "allow": "{{user.role == 'admin' or user.role == 'Admin'}}"
    }
  ],
  "properties": {
    "date": {
      "type": "string",
      "format": "date",
      "description": "Fecha del día festivo"
    },
    "name": {
      "type": "string",
      "description": "Nombre del día festivo"
    },
    "description": {
      "type": "string",
      "description": "Descripción opcional del festivo"
    }
  },
  "required": [
    "date",
    "name"
  ]
}

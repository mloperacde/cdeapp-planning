{
  "name": "DailyPresence",
  "type": "object",
  "row_level_security": [
    {
      "operation": "read",
      "allow": "{{user.role == 'admin' or user.role == 'Admin' or user.role == 'rrhh' or user.role == 'supervisor'}}"
    },
    {
      "operation": "create",
      "allow": "{{user.role == 'admin' or user.role == 'Admin'}}"
    },
    {
      "operation": "update",
      "allow": "{{user.role == 'admin' or user.role == 'Admin'}}"
    },
    {
      "operation": "delete",
      "allow": "{{user.role == 'admin' or user.role == 'Admin'}}"
    }
  ],
  "properties": {
    "employee_id": {
      "type": "string",
      "description": "ID del empleado en la base maestra"
    },
    "employee_name": {
      "type": "string",
      "description": "Nombre del empleado (desnormalizado)"
    },
    "employee_code": {
      "type": "string",
      "description": "Código del empleado en Cuco360 (desnormalizado)"
    },
    "record_date": {
      "type": "string",
      "format": "date",
      "description": "Fecha del fichaje (yyyy-MM-dd)"
    },
    "present": {
      "type": "boolean",
      "default": true,
      "description": "Si el empleado estuvo presente (tuvo al menos un fichaje en el día)"
    },
    "shift": {
      "type": "string",
      "description": "Turno detectado según hora de primera entrada (Mañana/Tarde)"
    },
    "first_entry": {
      "type": "string",
      "description": "Hora de primera entrada (HH:MM)"
    },
    "last_exit": {
      "type": "string",
      "description": "Hora de última salida (HH:MM)"
    },
    "check_in_count": {
      "type": "number",
      "description": "Número total de fichajes del día"
    },
    "source": {
      "type": "string",
      "default": "cuco360",
      "description": "Origen de los datos"
    }
  },
  "required": [
    "employee_id",
    "record_date"
  ]
}

{
  "name": "MLPrediction",
  "type": "object",
  "row_level_security": [
    {
      "operation": "read",
      "allow": "{{user.role == 'admin' or user.role == 'Admin' or user.role == 'rrhh' or user.role == 'supervisor'}}"
    },
    {
      "operation": "create",
      "allow": "{{user.role == 'admin' or user.role == 'Admin'}}"
    },
    {
      "operation": "update",
      "allow": "{{user.role == 'admin' or user.role == 'Admin'}}"
    },
    {
      "operation": "delete",
      "allow": "{{user.role == 'admin' or user.role == 'Admin'}}"
    }
  ],
  "properties": {
    "tipo_prediccion": {
      "type": "string",
      "enum": [
        "Rotación Empleado",
        "Optimización Turnos",
        "Cuello Botella Máquina"
      ]
    },
    "employee_id": {
      "type": "string"
    },
    "machine_id": {
      "type": "string"
    },
    "fecha_prediccion": {
      "type": "string",
      "format": "date-time"
    },
    "probabilidad": {
      "type": "number",
      "minimum": 0,
      "maximum": 100
    },
    "nivel_riesgo": {
      "type": "string",
      "enum": [
        "Bajo",
        "Medio",
        "Alto",
        "Crítico"
      ]
    },
    "factores_contribuyentes": {
      "type": "array",
      "items": {
        "type": "object"
      }
    },
    "datos_analisis": {
      "type": "object"
    },
    "recomendaciones": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "optimizacion_turnos": {
      "type": "object"
    },
    "cuello_botella": {
      "type": "object"
    },
    "activo": {
      "type": "boolean",
      "default": true
    }
  },
  "required": [
    "tipo_prediccion",
    "fecha_prediccion",
    "probabilidad",
    "nivel_riesgo"
  ]
}

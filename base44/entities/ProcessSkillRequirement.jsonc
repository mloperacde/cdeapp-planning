{
  "name": "ProcessSkillRequirement",
  "type": "object",
  "row_level_security": [
    {
      "operation": "read",
      "allow": "{{true}}"
    },
    {
      "operation": "create",
      "allow": "{{user.role == 'admin'}}"
    },
    {
      "operation": "update",
      "allow": "{{user.role == 'admin'}}"
    },
    {
      "operation": "delete",
      "allow": "{{user.role == 'admin'}}"
    }
  ],
  "properties": {
    "process_id": {
      "type": "string"
    },
    "skill_id": {
      "type": "string"
    },
    "nivel_minimo_requerido": {
      "type": "string",
      "enum": [
        "B\u00e1sico",
        "Intermedio",
        "Avanzado",
        "Experto"
      ],
      "default": "B\u00e1sico"
    },
    "obligatorio": {
      "type": "boolean",
      "default": true
    },
    "prioridad": {
      "type": "number",
      "default": 1
    }
  },
  "required": [
    "process_id",
    "skill_id",
    "nivel_minimo_requerido"
  ]
}